// Start with the initial value of thePowerof
let thePowerof = 3;

// Continue looping as long as thePowerof is less than 10000
while (thePowerof < 10000) {

   // Print the current value of thePowerof to the console
   console.log(thePowerof);

   // Multiply thePowerof by 3 and store the result back in thePowerof
   thePowerof = thePowerof * 3;
}
